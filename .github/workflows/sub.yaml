name: sub

on:
  workflow_run:
    workflows:
      - main
    types: 
      - completed

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: |
          echo "Hello World"
